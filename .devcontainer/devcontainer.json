{
	"image": "localhost/next/ledger-app-builder:lite",
	"remoteUser": "root",
	"customizations": {
		"vscode": {
			"extensions": [
				"iulian-radu-at.vscode-tasks-sidebar",
				"webfreak.debug",
				"ms-python.python",
				"ms-python.debugpy",
				"ms-vscode.cmake-tools",
				"ms-vscode.cpptools"
			]
		}
	}
	// vscode does not define XDG_SESSION_TYPE in the container
	// Qt applications, like speculos, default to X.ORG instead of wayland
	// We need to allow the container to open a window on the shared display
	// "initializeCommand": "xhost + local:",
	// Another approach is to query it from the host when running on linux
	// "containerEnv": {
	//	"XDG_SESSION_TYPE": "${localEnv:XDG_SESSION_TYPE}"
	// }
}